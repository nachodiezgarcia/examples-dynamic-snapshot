# Token de lectura de Content Island. Con token el api pide en vivo (activa /api/refresh); sin token sirve el snapshot del bundle.
CONTENT_ISLAND_READ_TOKEN=pon-aqui-tu-token

# Secreto que exige POST /api/refresh (cabecera x-refresh-secret).
REFRESH_SECRET=cambia-esto
